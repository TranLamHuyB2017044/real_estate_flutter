

import 'package:flutter/material.dart';

class TabletProfile extends StatelessWidget {
  const TabletProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child:const Text('heelo mobile profile')
    );
  }
}